%h1= t Newsbite.model_name.human(count: 2)
%table.table.table-hover
  %thead
    %tr
      %th= Newsbite.human_attribute_name(:title)
      /%th= Newsbite.human_attribute_name(:content)
      %th= Newsbite.human_attribute_name(:cover)
      %th= Newsbite.human_attribute_name(:published_at)
      %th= t 'status.status'
      %th= t 'actions.actions'

  %tbody
    - @newsbites.each do |newsbite|
      %tr
        %td= newsbite.title
        /%td= newsbite.content
        %td= image_tag newsbite.cover.thumb.url if newsbite.cover?
        %td= newsbite.published_at
        %td= status_mark newsbite.status
        %td= render 'admin/shared/index_buttons', resource: newsbite
%br
.row
  .col-sm-2
    = link_to t('actions.create'), new_admin_newsbite_path, class: 'btn btn-primary btn-block'
